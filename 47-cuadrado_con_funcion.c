#include <stdio.h>
#include <stdlib.h>
#include <math.h>
// Hernandez Hernandez Erik Jose
int main()
{
    int numero, resultado;
    printf("Ingrese un numero para elevarlo al cuadrado: ");
    scanf("%d", &numero);
    // resultado=pow(numero, 2);
    printf("%d", resultado = pow(numero, 2));
    return 0;
}
